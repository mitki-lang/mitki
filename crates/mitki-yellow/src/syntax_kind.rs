#[allow(non_camel_case_types)]
#[derive(Clone, Copy, Debug, Eq, Hash, PartialEq)]
pub enum SyntaxKind {
    LEFT_PAREN,
    RIGHT_PAREN,
    LEFT_BRACKET,
    RIGHT_BRACKET,
    LEFT_BRACE,
    RIGHT_BRACE,
    EQ,
    DOT,
    COMMA,
    COLON,

    FUN_KW,
    IF_KW,
    ELSE_KW,
    LOOP_KW,
    VAL_KW,
    WHILE_KW,
    NAME,

    INT_NUMBER,
    FLOAT_NUMBER,
    BINARY_OPERATOR,
    POSTFIX_OPERATOR,
    PREFIX_OPERATOR,

    UNKNOWN,
    EOF,

    BINARY_EXPR,
    LOOP_EXPR,
    IF_EXPR,
    ERROR,
    EXPR_STMT,
    FN,
    IDENT,
    LITERAL,
    MODULE,
    PARAM,
    PARAM_LIST,
    PAREN_EXPR,
    POSTFIX_EXPR,
    PREFIX_EXPR,
    STMT_LIST,
    TOMBSTONE,
    VAL_STMT,
}
